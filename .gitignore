node_modules/
.next/
out/
build/
dist/
*.log
.DS_Store
data/
next-env.d.ts

.env
.env.local
.env.*.local
!.env.example
tsconfig.tsbuildinfo
ruvector.db

# local-only secrets / scratch notes (never commit) + Claude Code worktrees
access.txt
.claude/worktrees/

# playwright
playwright-report/
test-results/
.next-e2e/
# Verification build (scripts/deploy.sh, NEXT_DIST_DIR) — never committed.
.next-verify/
